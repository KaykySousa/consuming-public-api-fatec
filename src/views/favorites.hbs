<div class="min-h-screen w-full flex flex-col justify-center mx-auto max-w-5xl p-6">
    <div class="flex flex-col justify-between gap-x-4 gap-y-2 min-[350px]:flex-row min-[350px]:items-center">
        <h1 class="font-serif text-4xl">Favoritos</h1>
        <a href="/" class="text-gray-500 hover:underline">PÁGINA INICIAL</a>
    </div>
    <div class="mt-6 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {{#each favorites}}
            <a href="/favorites/{{ idMeal }}">
                <div class="flex flex-col w-full h-full bg-white shadow rounded">
                    <img src="{{ strMealThumb }}" alt="{{ strMeal }} image" class="rounded-t w-full aspect-video object-cover" />
                    <div class="flex flex-col p-4">
                        <h1 class="font-serif text-3xl">{{ strMeal }}</h1>
                        <p class="uppercase text-gray-500 mt-1">
                            {{strCategory}} | {{strArea}}
                        </p>
                    </div>
                </div>
            </a>
        {{/each}}
    </div>
</div>